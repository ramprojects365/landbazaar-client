/**
 * Standard placeholder copy.
 *
 * - Example values: `e.g. Shadnagar` (what to type, not a repeat of the label)
 * - Actions / secrets: `Enter your password` (no fake password examples)
 */
export function examplePlaceholder(example: string): string {
  return `e.g. ${example}`;
}
